<%= form_for(item) do |f| %>
  <% if item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(item.errors.count, "error") %> prohibited this item from being saved:</h2>

      <ul>
      <% item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :wetWeight %>
    <%= f.text_field :wetWeight %>
  </div>

  <div class="field">
    <%= f.label :dryWeight %>
    <%= f.text_field :dryWeight %>
  </div>

  <div class="field">
    <%= f.label :ComposterUpdate_id %>
    <%= f.text_field :ComposterUpdate_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
